import { EnterNavigationDirective } from './enter-navigation.directive';

describe('EnterNavigationDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterNavigationDirective();
    expect(directive).toBeTruthy();
  });
});
